# Orb Salvager — Three.js + Cannon-ES (Hardened for GitHub Pages)
- CDN: jsDelivr pinned versions
- Controls: OrbitControls (drag to look), WASD to move, R reset
- No pointer lock required (works in restrictive environments)
